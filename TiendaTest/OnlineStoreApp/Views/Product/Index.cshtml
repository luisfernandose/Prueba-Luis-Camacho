@model DAO.Product
@{
    ViewBag.Title = "Index";
}

<h2>Productos</h2>


<form action="@Url.Action("NewProduct", "Product")" method="post">
    <div>
        <div>
            @Html.LabelFor(l => l.Producto)
            @Html.TextBoxFor(p => p.Producto)
        </div>
        <div>
            @Html.LabelFor(l => l.Precio)
            @*@Html.TextBoxFor(p => p.Precio, new { @class = "Valor Numérico", id = "Precio", Value = String.Format("{0:C}", Model.Precio) })*@
            @Html.TextBoxFor(p => p.Precio)
        </div>
        <div>
            @*@Html.DropDownListFor(c => c.Category, new SelectList(Model.Category, "Id", "Categoria"))*@
        </div>
        <div>
            <button type="submit">Nuevo</button>
        </div>
    </div>

</form>